import PlaygroundExperimentMode from "@/components/dashboard/PlaygroundExperimentMode";

export default function PlaygroundPage() {
  return (
    <div className="h-[calc(100vh-4rem)] sm:h-[calc(100vh-5rem)] w-full">
      <PlaygroundExperimentMode />
    </div>
  );
}
